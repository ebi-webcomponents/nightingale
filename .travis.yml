language: node_js
node_js:
  - "node"
script:
  - npm run bootstrap
  - npm run build
before_deploy:
  - npm run build-site
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: app/dist
  on:
    branch: master
